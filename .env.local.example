# Partykit Configuration
# After deploying to Partykit, add the URL here
NEXT_PUBLIC_PARTYKIT_HOST=localhost:1999

# Optional: Password for host authentication
# If not set, first player to join becomes host automatically
# HOST_PASSWORD=adminpass123
